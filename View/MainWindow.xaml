<Window
    x:Class="QTIParserApp.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:QTIParserApp.View"
    xmlns:model="using:QTIParserApp.Model"
    Title="QTI Parser">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Load QTI File"
                Click="LoadQTIFile_Click"
                Margin="10"
                Grid.Row="0" />

        <TextBlock Text="{Binding CurrentQuiz.Title}"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="10,5,10,10"
                   TextAlignment="Center"
                   HorizontalAlignment="Center"
                   Grid.Row="1" />

        <ListView ItemsSource="{Binding FormattedQuestions}"
                  Grid.Row="2"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:FormattedQuestion">
                    <StackPanel Margin="10">
                        <TextBlock Text="{x:Bind QuestionType}" 
                   FontWeight="Bold" 
                   FontSize="16"/>
                        <muxc:WebView2 Source="{x:Bind Text}"
                       Width="750"
                       Height="150"
                       NavigationStarting="QuestionWebView_NavigationStarting" />
                        <!-- New: Display Answers -->
                        <TextBlock Text="Answers:" 
                   FontWeight="SemiBold" 
                   Margin="10,5,10,0"/>
                        <ItemsControl ItemsSource="{x:Bind Answers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="model:Answer">
                                    <TextBlock Text="{x:Bind Text}" 
                               Foreground="DarkBlue"
                               Margin="10,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ItemsControl ItemsSource="{x:Bind Attachments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="model:QuestionAttachment">
                                    <TextBlock Text="{x:Bind FilePath}"
                               Foreground="LightGray"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>

